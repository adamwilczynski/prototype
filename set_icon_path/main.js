// Icons derived from Emoji One BW icons

'use strict';

var currentUrl = window.location.hostname.toLowerCase();

console.log(currentUrl);
// chrome.browserAction.setIcon({path: 'icon2.png'});
// chrome.browserAction.setIcon({path: 'icon2.png'});
